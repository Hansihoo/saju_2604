# Project Status

| Feature Name | Feature Description | Progress Status | Notes |
| --- | --- | --- | --- |
| Local Codex LLM provider | Allow the saju phrasing layer to use local `codex exec` instead of the OpenAI API. | Done | Added `SAJU_LLM_PROVIDER=codex` path for full report, free preview, and detail render. Actual local Codex calls passed for free preview and full report; related unit tests passed. |
| Result pipeline hardening | Remove duplicate LLM report generation and make provider status, score exposure, calculation confidence, and operational safeguards production-ready. | Needs review | Initial requests now generate only the free preview; full interpretation is lazy. Codex success is recognized, heuristic scores are internal-only, legacy primary calculation remains selected, and PII-safe logging/cache/timeout controls are in place. API suite (232), final focused suite (74), web build, and local browser/API flow passed. Vercel WAF must be staged log-only and published by the project owner using `docs/operations/PUBLIC_BETA_RUNBOOK.md`. |
| Result design lab | Compare document-style treatments using a fixed saju result without API or Codex calls. | Done | Reworked `/design-lab` around premium report, essay report, and one-page brief variants. `pnpm build:web` passed; desktop and 390px mobile browser checks showed no console errors or horizontal overflow. |
| Essay result layout | Apply the document-lab essay treatment to the live saju result page. | Done | Added `essay-result` mode to `SajuResultView` and restyled the result shell, outline, hero, insight cards, narrative sections, and data cards as a document-style reading. `pnpm build:web` passed. |
| Modern ritual result theme | Apply the selected third design direction to the live result page with deep navy, bronze accents, and mobile-first reading hierarchy. | Done | Added scoped `ritual-page-shell`/`ritual-result` styling without changing calculation or response behavior. 390px mobile and 1440px desktop browser checks passed with no console errors or horizontal overflow; preview/detail screenshots saved under `docs/ai/screenshots/`. |
| Result readability refinement | Correct the first visual pass's typography, line wrapping, contrast, and reading hierarchy for Korean users. | Done | Removed decorative serif treatment from result text, standardized Korean sans-serif typography, moved long-form content to a light reading surface, limited headline/body measure, and kept navy/bronze as restrained accents. Removed the dark hero, dark outline panel, and dark table headers so the entire result screen is light. PC/mobile screenshots refreshed and `pnpm build:web` passed. |
| Overflow-safe result typography | Prevent long Korean/LLM copy from breaking the result layout across viewport sizes. | Done | Added shared `overflow-safe` rules for min-width, responsive wrapping, balanced headings, flexible buttons, and intentional table scrolling. Added a development-time ResizeObserver/MutationObserver audit; PC 1440px and mobile 390px preview/detail flows passed with zero detected issues. |
| Project documentation refresh | Make the project identity, recent changes, and no-OpenAI-cost local testing path easier to find. | Done | Updated README and PROJECT_PROFILE, added `docs/START_HERE.md`, and documented fallback/Codex local test modes versus OpenAI production default. |
